# ── Web framework / server ───────────────────────────────────────────────────
fastapi
uvicorn
python-multipart
slowapi
httpx

# ── Config & validation ──────────────────────────────────────────────────────
pydantic
python-dotenv

# ── Persistence ──────────────────────────────────────────────────────────────
sqlalchemy
aiosqlite
# PostgreSQL drivers (async for the web app, sync for the Celery worker)
asyncpg
psycopg2-binary
alembic

# ── Async job system (redesign §11) ──────────────────────────────────────────
celery[redis]
redis

# ── Auth & security ──────────────────────────────────────────────────────────
passlib[bcrypt]
bcrypt<4.1
python-jose[cryptography]
cryptography

# ── LLM / agent ──────────────────────────────────────────────────────────────
# Agent uses native function calling: Groq (hosted default) → Gemini → Ollama (local).
groq
google-genai
ollama

# ── Materials science core ───────────────────────────────────────────────────
ase
pymatgen
mp-api

# ── Machine-learned interatomic potentials ───────────────────────────────────
# torch is installed CPU-only in the Dockerfile (no CUDA). MACE is the default
# calculator; MatterSim is the alternative. Both reuse the pre-installed CPU torch.
# Versions PINNED to the combo proven to work together (matches the dev venv):
# MACE pins e3nn==0.4.4; MatterSim's metadata over-declares e3nn>=0.5.0 but runs
# fine on 0.4.4 — so we pin e3nn and install with pip's legacy resolver (Dockerfile).
mace-torch==0.3.16
mattersim==1.2.4
e3nn==0.4.4

# ── Scientific stack ─────────────────────────────────────────────────────────
numpy
matplotlib

# ── HTTP utilities ───────────────────────────────────────────────────────────
requests

# ── NOTE: re-add per upcoming tool (kept out to stay lean) ───────────────────
# pymatgen-analysis-defects   # when building vacancy/substitution/interstitial tools
# icet                        # when building SQS generation
