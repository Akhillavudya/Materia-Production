"""Structure-building service (Step 5.5).

Pure pymatgen transforms (supercell / vacuum / slab / convert) and analysis
(symmetry, defects). The agent tools in ``app.tools.material_tools`` adapt these
to the session/file layer.
"""
